# VS Code Setup

This directory contains a nested Gradle project. For detailed VS Code setup instructions, see [VSCODE_SETUP.md](../VSCODE_SETUP.md) in the parent directory.

**Quick fix if Java files aren't recognized:**
1. Command Palette → "Java: Clean Java Language Server Workspace"
2. Command Palette → "Java: Reload Projects"
