plugins {
    id 'java'
    id 'application'
}

dependencies {
    implementation project(':libs:core-utils')
}

application {
    mainClass = 'com.example.apps.CliApp'
}

tasks.register('showInfo') {
    doLast {
        println 'CLI App - depends on core-utils'
    }
}
