# Lesson 5: Handling Dependencies

## Overview
Dependencies are managed in configurations like `implementation`, `testImplementation`.

## Repositories
- `mavenCentral()`, `jcenter()`, custom repos.

## Exercise Solution
Add `implementation 'com.google.guava:guava:31.1-jre'` and use Guava in code.
